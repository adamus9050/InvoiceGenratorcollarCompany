@model InvoiceGeneratorCollarCompany.Models.DTOs.ProductDisplayModel

@{

}
<div my-2>
   
 <form asp-action="Index" class="row gx-3 gy-2 align-items-center">
    @* Wyszukiwanie po tytule*@
   <div class="col-sm-3">
     <label class="visually-hidden" for="sterm">Szukaj po nazwie produktu</label>
     <div class="input-group">
       <div class="input-group-text"></div>
       <input type="text" class="form-control" value="@Model.STerm" id="sterm" name="sterm" placeholder="Szukaj po nazwie produktu">
     </div>
   </div>
 @*  Wybieranie typów labelka*@
    <div class="col-auto">
     <label class="visually-hidden" for="typeId">Types</label>
     <select class="form-select" id="typeId" name="typeId">
       <option selected>Wybierz Typ</option>
       @foreach(var types in Model.Type )
       {
            <option selected="@(types.Id ==Model.TypeId)" value="@types.Id">@types.TypeName</option>         
       } 

     </select>
   </div>

   <div class="col-auto">
     <button type="submit" class="btn btn-primary">Szukaj</button>
   </div> 
 </form>
  
</div>


     <div class="w-100 mt-4 d-flex flex-wrap">
    @foreach (var product in Model.Product)
    {

      <div class="card mx-2 mb-4" style="width: 16rem;">
            @if (string.IsNullOrEmpty(product.Image))
            {
                  <img width="100%" height="300px" src="/images/NoImage.png" class="card-img-top" alt="...">       

            }
            else
            {
                  <img src="/image/@product.Image" class="card-img-top" alt="...">       
            }

          <div class="card-body ">
          <h5 class="card-title"> @product.ProductName</h5>
          <p class="card-text">
                

           <div class="col-auto">
            <label class="visually-hidden" for="autoSizingSelect">Choose</label>
            <select class="form-select" id="sizeId">
              <option selected>Wybierz rozmiar</option>
@*              @foreach(var size in product.Sizes) // Model.product.size
              {
                <option value="@size.Id ">@size.NameInt :: Idproduktu=@size.ProductId </option>
              }</select>*@


             <select class="form-select" id="MaterialId">
              @foreach(var material in product.Materials)
              { 
@*                   @if (string.IsNullOrEmpty(product.Image))
                   {
                       <img width="15px" height="15px" src="/images/NoImage.png" class="card-img-top" alt="...">     

                   }
                   else
                   {
                       <img src="/image/@product.Image" class="card-img-top" alt="...">       
                   }*@
                <option value="@material.MaterialId">@*@material.Name @material.Colour*@
@*                    @if (string.IsNullOrEmpty(material.ImageColour))
                    {
                       <img width="15px" height="15px" src="/images/NoImage.png" class="card-img-top" alt="...">     

                    }
                    else
                    {*@
                        <img width="15px" height="15px" src="/image/Colours/@material.ImageColour" class="card-img-top" alt="...">       
                    @*}*@
                            <div class="d-flex">
          <div class="p-2 w-100">@material.Name @material.Colour</div>
                  <div class="p-2 flex-shrink-1">
                      <img width="15px" height="15px" src="/image/Colours/@material.ImageColour" class="card-img-top" alt="...">       
                  </div>
        </div>
                </option>
              }
            </select>

            </div>  <br/>
             Opis:@product.Description<br/>
             Typ: @product.Type.TypeName<br/>
             Cena:@product.ProductPrice<br/>
          </p>


            <a href="#" type="button" class="btn-primary">Dodaj do koszyka</a>
      </div>
      </div>
    }
</div>